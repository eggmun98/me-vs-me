/**
 * 약관과 방침에 이름으로 등장하는 값들.
 *
 * ⚠️ 서비스를 공개하기 전에 아래 `채워야 한다` 표시가 붙은 값을 전부 실제 값으로 바꾼다.
 * 개인정보처리방침에 보호책임자와 연락처를 적는 것은 개인정보보호법 제30조가 정한
 * 의무 기재사항이라, 비워둔 채로 배포하면 방침을 게시하지 않은 것과 같다.
 */
export const OPERATOR = {
  /** 서비스 이름. 앱·웹에 보이는 이름과 같아야 한다. */
  serviceName: "나 VS 나",

  /** 운영자(개인 또는 상호). 채워야 한다. */
  name: "[운영자 이름]",

  /** 이용자 문의·열람청구를 받는 주소. 채워야 한다. */
  contactEmail: "[문의 이메일]",

  /** 개인정보 보호책임자. 1인 운영이면 운영자와 같은 사람을 적는다. 채워야 한다. */
  privacyOfficer: "[보호책임자 이름]",

  /** 서버를 빌려 쓰는 사업자. 위탁 항목에 적힌다. 배포처를 정한 뒤 채워야 한다. */
  hostingProvider: "[클라우드 사업자]",

  /** 두 문서의 시행일. 개정할 때마다 올린다. */
  effectiveFrom: "2026-09-01",
} as const;
